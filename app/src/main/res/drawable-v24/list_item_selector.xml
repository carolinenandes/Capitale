<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Estado normal (não selecionado) -->
    <item android:drawable="@android:color/transparent" />

    <!-- Estado pressionado (selecionado) -->
    <item android:state_pressed="true" android:drawable="@color/selecionado" />

    <!-- Estado focado (quando o item ganha foco) -->
    <item android:state_focused="true" android:drawable="@color/selecionado" />
</selector>